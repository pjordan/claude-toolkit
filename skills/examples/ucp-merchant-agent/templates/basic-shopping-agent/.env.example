# UCP Shopping Agent Configuration

# Agent profile URL - must be publicly accessible
# This URL should return your agent's profile JSON
AGENT_PROFILE_URL=https://your-agent.example/profile.json

# Server configuration
HOST=0.0.0.0
PORT=8000

# Optional: Logging level
LOG_LEVEL=INFO

# Optional: Request timeout (seconds)
REQUEST_TIMEOUT=30

# Optional: Redis for session storage (production)
# REDIS_URL=redis://localhost:6379/0

# Optional: AP2 key path for autonomous payments
# AP2_PRIVATE_KEY_PATH=/path/to/private_key.pem
